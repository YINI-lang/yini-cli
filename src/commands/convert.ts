export type TConvertFormat = 'JSON' | 'YAML' | 'XML'

export function convertFile(file: string, format: TConvertFormat): void {
    console.log(`Converting ${file} to ${format}...`)
    // TODO: Add format conversion logic.
    console.log(`--TODO: command: convert--`)
}
