^ App
    ^^ window
    title = 'Sample Window'  // Strings can be enclosed in either ' or ".
    id = 'window_main'

    ^^ image
    src = 'gfx/bg.png'
    id = 'bg1'
    isCentered = true

// INVALID in strict mode: the required document terminator is missing.
// The line below is disabled and therefore does not count as /END.
--/end
